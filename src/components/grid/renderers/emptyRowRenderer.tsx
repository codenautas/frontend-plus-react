import React from 'react';

// Este componente ya no se usa directamente para el fallback de RDG
// pero lo mantenemos por si se desea usar en otro lado o por compatibilidad.
export function EmptyRowsRenderer() {
  return null;
}